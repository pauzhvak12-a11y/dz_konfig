# Учебный конфигурационный язык (вариант №28)

## 1. Общее описание
Инструмент командной строки на Python, который читает текст на учебном конфигурационном языке из `stdin`, преобразует его в JSON и выводит в `stdout`. При лексических или синтаксических ошибках выводит сообщение в `stderr` и завершает работу с кодом 1. Для синтаксического разбора используется специализированный парсер Lark (LALR).

## 2. Описание функций и настроек
- Поддерживаемые конструкции языка:
  - Многострочные комментарии: `{{! ... }}`.
  - Числа: `0` или восьмеричные `0[o0][0-7]+`.
  - Имена (идентификаторы): `[a-z][a-z0-9_]*`.
  - Массивы: `list( значение, значение, ... )` (допускается пустой список `list()`).
  - Словари:
    ```
    begin
      имя := значение;
      ...
    end
    ```
  - Объявления констант: `имя = значение`. Программа состоит из набора таких объявлений; имена становятся ключами итогового JSON.
  - В выражениях можно ссылаться на ранее объявленные константы по имени.
- Основные файлы:
  - `config_lang.py` — грамматика Lark, синтаксический разбор и вычисление констант.
  - `main.py` — CLI для запуска преобразования.
- Зависимости:
  - `lark-parser==0.12.0`
  - стандартная библиотека Python 3.10+

## 3. Команды сборки и запуска тестов
- Запуск конвертации (чтение из `stdin`, вывод в `stdout`):
  ```bash
  python main.py < input.conf > output.json
  ```
- Установка зависимостей:
  ```bash
  pip install -r requirements.txt
  ```
- Запуск тестов:
  ```bash
  python -m unittest
  ```

## 4. Примеры использования
- Пример конфигурации веб-сервера: `examples/web_server.conf`
- Пример конфигурации IoT-устройства: `examples/iot_device.conf`
- Где можно применять:
  - Учебные кейсы по формальным языкам и парсерам.
  - Простейшие декларативные конфигурации для сервисов или устройств.
  - Демонстрация трансляции в JSON для интеграции в пайплайны.
- Быстрый пример inline:
  ```text
  x = 0o10
  y = list(0, x)
  cfg = begin
    size := x;
    items := y;
  end
  ```
  Вывод:
  ```json
  {
    "x": 8,
    "y": [0, 8],
    "cfg": {
      "size": 8,
      "items": [0, 8]
    }
  }
  ```
##5 Фотоотчет по тестам

<img width="830" height="134" alt="Снимок экрана 2025-12-18 в 12 33 07" src="https://github.com/user-attachments/assets/c776c831-9c03-4228-aea2-f73cc9f7b584" />

Все тесты выполняются, программа работает корректно.
