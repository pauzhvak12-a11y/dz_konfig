{{! Пример конфигурации веб-сервера }}

http_port = 080
https_port = 0443

ports = list(http_port, https_port)

routes = begin
  root := begin
    path := 0;
  end;
  api := begin
    path := 0o1;
  end;
end

server = begin
  ports := ports;
  routes := routes;
end


